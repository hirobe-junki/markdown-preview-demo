<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/sanitize.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
  </head>
  <body class="container">
    <header>
      <h1 class="display-4 text-primary"><%= title %></h1>
    </header>
    <main>
      <p class="h5"><%= head %></p>
      <form method="post" action="/md/mark">
        <div class="form-groups">
          <input type="hidden" name="id" id="id" value="<%= id %>" />
          <div class="form-group">
            <label for="source">SOURCE</label>
            <textarea id="source" name="source" rows="5" class="form-control"><%= source %></textarea>
          </div>
        </div>
        <input type="submit" value="更新" class="btn btn-primary" />
      </form>
      <div class="card mt-4">
        <div class="card-header tex-center h5">
          Preview
        </div>
        <div class="card-body">
          <%- content %>
        </div>
        <div class="card-footer text-muted text-right">
          <%= footer %>
        </div>
      </div>
      <p class="mt-4"><a href="/md">&lt;&lt; Top へ戻る</a></p>
    </main>
  </body>
</html>
